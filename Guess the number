#Guess the number assignment
#By Chase Cole
#11/21/21

#This game will choose a random number and the player will have to attempt to pick the correct number
#importing random function
import random

#prints welcoming to player and the goal
print("Welcome to the random number game")
print("Your goal is to guess the correct number")
#setting up loop for replay
yes = "y"
replay = str("y")
while (replay == yes):
    
#assigns input for player to guess    
#assigns varible to the amount of guesses taken to achieve victory
    attempt = int(input("Guess a number!"))
    guesses = 1
#creates the random number that the player is trying to guess
    lucky_number = random.randint(1,100)
#shows if guess is higher or lower, prints accordingly
#second loop for multiple guesses until correct number is found
    while attempt != lucky_number:
        if attempt > lucky_number:
            print("lower")
        else:
            print("higher")
#add attempts and allow for multiple guesses
        attempt = int(input("Guess a number!"))
        guesses += 1
#displays when correct and will ask if wants to play again
    else:
        
        print("Correct!")
        print("Number of attempts taken " + str(guesses))
        replay = str(input("Play again? y for yes and n for no "))
#closes first loop, closes program and displays thanks
else:
    print("Thanks for playing!")

